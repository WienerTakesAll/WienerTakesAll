#pragma once

enum PowerupType {
    KETCHUP,
    RELISH,
    MUSTARD,
    INVINCIBILITY,
    POWERUP_COUNT
};

enum PowerupTarget {
    SELF,
    OTHERS
};

enum RelishType {
    DOMINATE,
    REVERSE,
    RESTORE
};
